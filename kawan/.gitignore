# Node / frontend
node_modules/
dist/
*.local
.vite/

# Python / backend
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# SQLite
*.db
*.sqlite3

# Env & secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/

# Logs
*.log

# Piper TTS voice models (.onnx + .onnx.json) — large binary files, never committed.
# Run scripts/download_voices.sh to populate locally.
backend/voices/

# Live2D model files are now tracked via Git LFS (kawan/.gitattributes).
# The PO stages and commits the model trees after running `git lfs install`.
